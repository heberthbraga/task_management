<div class="flex flex-col">
  <%= render AssetsNavbarComponent.new(options: { 
    heading: 'activities.heading', 
    button_path: 
    activities_path, 
    button_title: 'activities.add_button', 
    frame_id: 'activities_container' } ) 
  %>
  
  <div id="activities_container" class="flex flex-col bg-white mx-10 my-8 rounded-lg px-4">
    <%= render TabPaneComponent.new(pane: activities_pane(12, 8, 4)) %>
   
    <div id="pane_content" class="px-4 py-6">
      <%= render 'cards' %>
    </div>
  </div>
</div>