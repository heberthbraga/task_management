<header class="flex items-center justify-around bg-gray-vintage px-2 py-3">
  <div>Logo</div>
  <div>About</div>
  <% if user_signed_in? %>
  <% else %>
    <div>Sign In</div>
    <div>Sign Up</div>
  <% end %>
</header>